{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🚀 OmniStock: 重建并启动 (Up & Build)",
            "type": "shell",
            "command": "docker compose up -d --build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new",
                "clear": true
            }
        }
    ]
}