# 1. 密钥与本地配置
.env

# 2. 虚拟环境
.venv/
venv/
env/

# 3. Python 缓存与编译文件
__pycache__/
*.pyc
*.pyo
*.pyd

# 4. macOS 系统文件
.DS_Store

# 5. 我们之前写的本地沙箱数据目录（防止把生成的报告传上去）
agent_workspace/
knowledge_base/
embeddings/
memory/
jobs/

# 6. OpenCode 架构方案目录（自动生成的临时设计文档）
.opencode/

# 7. Claude Code 本地配置（本机授权记录，不应共享）
.claude/

# === VS Code 配置文件 ===
# 1. 默认屏蔽整个目录（防止把你个人的字体大小、主题颜色等配置传上去）
.vscode/*

# 2. 用白名单机制（感叹号）强制放行“工程资产”
!.vscode/tasks.json    # 保留咱们的一键 Docker 构建快捷键
!.vscode/launch.json   # (如果未来你配了断点调试，这个也要保留)
!.vscode/extensions.json # (可选：如果你规定了该项目必须装哪些插件，可以保留)